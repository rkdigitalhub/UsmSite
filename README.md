# USM Site

Static marketing site for Unispace Market.

## Active files

- `index.html` is the current landing page entry point.
- `styles.css` contains the shared page styles.
- `script.js` handles the mobile menu, banner carousel, and reveal animations.

## Note

`page.html` is a legacy exported page snapshot and is not part of the current lightweight landing page flow.
